const initialState = {
    places:[], 
    selectedPlace: null,
};

const reducer=(oldState=initialState, action)=>{
    switch(action.type){
        default:
                return state;
    }
};

export default reducer;